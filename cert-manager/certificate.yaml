apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: springboot-cert
  namespace: default

spec:
  secretName: springboot-tls

  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer

  commonName: spring.local

  dnsNames:
    - spring.local
