apiVersion: v1
kind: Service
metadata:
  name: spring-service
  labels:
    app: springbootdemo
spec:
  type: ClusterIP
  selector:
      app: springbootdemo

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
